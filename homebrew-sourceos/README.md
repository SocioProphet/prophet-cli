# homebrew-sourceos

Homebrew distribution tap for SourceOS artifacts.

## Distribution-only scope
- This tap distributes binaries.
- Bootstrap engine build source lineage is `sourceos-sdk/cmd/sourceos-bootstrap`.
- `prophet-cli` is façade-only and not the bootstrap engine source home.
