# Import Issues

1. Import CSV task IDs into tracker with `phase1` labels.
2. Keep `prophet-cli-existing` marked as patch-only target.
3. Preserve frozen object boundaries and ownership from `docs/REPO_MATRIX.md`.
